from setuptools import find_packages, setup

setup(
      name='RLA',
      version='1.0.0',
      include_package_data=True,
      install_requires=[
                        'flask',
                        ],
      )
